---
import { LANGS } from "src/site-config";
export async function getStaticPaths() {
  return LANGS.flatMap((lang) => ({
    params: {
      lang,
    },
  }));
}

const { lang } = Astro.params;
---

<head>
  <meta http-equiv="refresh" content={`0; URL=/${lang}/posts/page/1`} />
</head>
